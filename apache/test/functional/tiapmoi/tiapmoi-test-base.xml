<?xml version="1.0"?>
<project name="test.tiapmoi.base">
  <description>
   Base file of all tiapmoi tests.
  </description>

  <!-- ADDED-BY-SEED Absolute path to this file -->
  <dirname property="test.tiapmoi.base.dir" file="${ant.file.test.tiapmoi.base}" />

  <!-- ADDED-BY-SEED Import tiapmoi base file -->
  <import file="${test.tiapmoi.base.dir}/tiapmoi-base.xml" />

  <!-- Import TLR core to run with TestLogic -->
  <import file="${testtool.3rdparty.testlogic.dir}/tlr-core-root.xml"/>

  <!-- ADDED-BY-SEED Import common test file targets -->
  <import file="${tl.cafe.file}"/>

  <!-- ====================================================================
    START: ADDED-BY-SEED Template generated code. Don't edit
   ===================================================================== -->
  <sequential>
    <!--
      Runs setup if it's neven been run. This is to help users to come to
      any test file and just run the target. Setup is run, if it's neven
      been run.
     -->

    <if>
      <not><available file="${team.setup.marker.file}"/></not>
      <then>
        <ant antfile="${team.base.dir}/build.xml"
             target="setup"
        />
      </then>
    </if>

  </sequential>
  <!-- ====================================================================
    END: ADDED-BY-SEED: Template generated code. Don't edit
   ===================================================================== -->

   <!-- Set path to find all tiapmoi goldfiles -->
   <!-- Platform dependent -->
   <property name="gold.genpath" value="${team.base.dir}/common/goldfiles"/>
   <if>
     <isset property="isWindows"/>
     <then>
       <property name="gold.path" value="${team.base.dir}/common/goldfiles/nt"/>
       <property name="platform" value="nt" />
     </then>
     <else>
       <property name="gold.path" value="${team.base.dir}/common/goldfiles/linux"/>
       <property name="platform" value="linux"/>
     </else>
   </if>


</project>
